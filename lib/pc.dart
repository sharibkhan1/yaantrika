import 'package:flutter/material.dart';

class Pcpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PC page'),
      ),
      body: Center(
        child: Text('This is the mobile Page'),
      ),
    );
  }
}